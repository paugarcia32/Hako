{
  "name": "@hako/db",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "db:migrate": "dotenv -e ../server/.env -- prisma migrate dev",
    "db:push": "dotenv -e ../server/.env -- prisma db push",
    "db:studio": "dotenv -e ../server/.env -- prisma studio",
    "db:migrate:test": "dotenv -e ../server/.env.test -- prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@hako/shared": "workspace:*",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.0.0",
    "typescript": "^5.5.0"
  }
}
