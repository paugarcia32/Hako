import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Archive — Inkbox',
  description: 'Your archived items.',
};

export default function ArchivePage() {
  return <h1>Archive</h1>;
}
